{
  "name": "courtzon-v2",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/shared"
  ],
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ci": "playwright test --grep \"Smoke — public\"",
    "validate:arch": "node scripts/architecture/validate-all.js",
    "ci": "node scripts/ci-validate.js && node scripts/architecture/validate-all.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
